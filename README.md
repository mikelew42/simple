The <base> and /site-name/ paths (for /site-name/app.js in 404.html) are setup for a GitHub Pages sub-site.  If you use this for a root site, you can fix them:

- remove the <base> tag
- change /site-name/app.js to /app.js